# Issue Tracker

Issues for this repo live in GitHub Issues for `infomiho/buzz-static-hosting`.

Use the `gh` CLI from the repo root.

Create issues with:

```bash
gh issue create --title "<title>" --body-file <file>
```

When publishing dependent issues, create blockers first so later issues can reference real issue numbers.
