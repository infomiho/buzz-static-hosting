# Triage Labels

This repo does not use triage labels.

When creating issues, do not apply readiness/state labels such as `needs-triage`, `ready-for-agent`, or `ready-for-human` unless the user explicitly asks for labels.
