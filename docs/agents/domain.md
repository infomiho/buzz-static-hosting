# Domain Docs

This repo does not currently have dedicated domain docs.

Use `CLAUDE.md` as the current project context. If `CONTEXT.md` or `docs/adr/` are added later, skills should read those before drafting architecture, diagnosis, TDD, or issue-breakdown work.
